# About

Subtitle pattern: `{role} · {location}`

Footer: Currently in: agentic AI, recommendation systems, RAG pipelines.

> Content is sourced from `RESUME.summary` and `RESUME.role` / `RESUME.location`.
> Photo is embedded as base64 in the JSX.
